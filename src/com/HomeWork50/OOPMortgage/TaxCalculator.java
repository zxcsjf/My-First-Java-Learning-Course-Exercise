package com.HomeWork50.OOPMortgage;

public interface TaxCalculator {
    // Abstract method without any declaration, no { }.
    double calculateTax();

    // Public in grayed out,
    // No need to write,
    // All method must implements by class,
    // So it is public contract.
    public static void doSometing(){}
}
